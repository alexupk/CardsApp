import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class FlatCards extends Component {
  render() {
    return (
      <View>
        <Text> textInFlatCards </Text>
      </View>
    )
  }
}

const styles = StyleSheet.create({})

